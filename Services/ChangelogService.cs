namespace BlindNotepad.Services;

/// <summary>
/// 更新日志服务。提供内置的版本更新记录。
/// </summary>
public static class ChangelogService
{
    /// <summary>获取完整更新日志文本。</summary>
    public static string GetFullChangelog()
    {
        return """
            随心记 更新日志
            ================

            v2.4.7 (2026-08-08)
            ─────────────────
            [修复] ALT 菜单找回：上一版错误地给 Menu 和所有 MenuItem 加了 Focusable=False /
                   IsTabStop=False，导致菜单即使显示也无法接收焦点和操作。现已移除这些限制，
                   保留 Visibility=Collapsed（默认隐藏，TAB 不可达）；按 ALT 时显示并聚焦
                   第一个菜单项，可用方向键/TAB/字母助记键正常导航；再按 ALT 或 ESC 隐藏。
            [优化] 活动区（状态栏）内容暂时全部清空，等后续指示再添加

            v2.4.6 (2026-08-08)
            ─────────────────
            [修复] 自动更新死循环：发布构建中 AssemblyInformationalVersion 带有 "+<commit>" 后缀（例
                   如 "2.4.5+325fd32"），Version.TryParse 无法解析而退化为字符串比较，导致
                   "v2.4.5" vs "2.4.5+xxx" 比较后仍被判定为"有新版本"，新程序每次启动都会重
                   复提示更新。修复：CurrentVersion 和 IsNewerVersion 统一先去掉 '+' 及后
                   缀，再按 Version 类强类型比较。
            [修复] 更新 bat 等待进程退出更可靠：用 wmic process where ProcessId= 精准确认 PID
                   是否存活（tasklist|find 会误匹配包含同数字的其他进程）；等待间隔改为 1 秒
                   （timeout 命令更精确）；最大等待 30 秒，超时后 taskkill /F 强制杀掉主进
                   程，避免占用中的 exe 复制失败又无声无息。

            v2.4.5 (2026-08-08)
            ─────────────────
            [修复] Ctrl+6 切换到记账模块出现空引用异常：SwitchToModule 整体加 try-catch 兜底，快捷键切换用 SafeSet 包装；出现异常写入 data/error.log 不再崩溃弹窗
            [优化] 菜单栏默认隐藏；按 ALT 显示菜单；再按一次 ALT 或 ESC 隐藏菜单
            [优化] 主界面 TAB 键导航精简：TAB 只会在 分类树 → 当前列表 → 添加按钮 之间循环
                   模块切换 RadioButton / 搜索框 / 清除按钮 / 两个分隔条 / 详情内容 / 状态栏 全部不可 TAB 到达
                   分类树用方向键走；F6 仍然可以在分类树/列表/详情区之间切换焦点

            v2.4.4 (2026-08-07)
            ─────────────────
            [修复] 自动更新：下载后自动安装并重启；保留 data/ 目录；robocopy 复制更可靠；出错弹框提示
            [迁移] 用户数据、缓存、草稿、快捷键配置全部从 C 盘 %LocalAppData%/SuixinJi 迁移到程序目录下的 data/ 文件夹（方便您直接找到）；首次启动自动迁移旧数据
            [优化] 新建网址/密码/证件/记账/笔记时的分类、标签下拉框不再重复播报（争渡会根据控件类型自动读，去掉了冗长的 AutomationProperties.Name）
            [优化] Ctrl+Enter 复制网址、复制名称、复制账号时不再有额外语音提示（音效反馈即可）

            v2.3.0 (2026-08-07)
            ─────────────────
            [新增] 全局搜索功能（Ctrl+Shift+F）
              - 跨所有模块全文搜索
              - 公开/私密/全部三种范围筛选
              - 多字段匹配预览、分组统计
              - F3 跳转模块分组
            [新增] 自动更新（无需打开浏览器）
              - 一键下载安装包并自动替换重启
              - 下载进度对话框，支持取消
              - 更新时自动清除旧版本文件
              - 无安装包时降级为打开网页
            [修复] 启动崩溃（PinkDeep Color 误用为 Brush）
            [移除] 话本模块

            v2.2.0 (2026-08-07)
            ─────────────────
            [优化] 粉色系可爱风格界面主题
            [优化] 软件更名为"随心记"

            v2.0.0 (2026-08-07)
            ─────────────────
            [新增] 查找替换功能（Ctrl+H，支持正则表达式）
              - 记事本和日记编辑器均支持
              - 区分大小写、正则表达式选项
              - 查找下一个/上一个、替换、全部替换
            [新增] 朗读功能（Windows SAPI）
              - 朗读选中文本或全部内容
              - 可随时停止朗读
            [新增] 语音输入（Windows 听写）
              - 一键启动 Win+H 听写
              - 语音转文字插入光标位置
            [新增] 排版功能
              - 多种预设排版样式
              - 支持公众号、视频号等平台格式
            [新增] 总结功能
              - 自动提取文章主要内容
              - 提取标题/标签供发布使用
              - 支持拷贝到剪贴板
            [新增] 字数统计（取消字数限制）

            v1.4.0 (2026-08-07)
            ─────────────────
            [新增] 自动草稿保存（记事本和日记模块）
              - 每 30 秒自动保存草稿到磁盘
              - 重新打开时提示恢复未保存内容
              - 保存成功后自动清除草稿
            [新增] 自动更新检测
              - 通过 GitHub Releases API 检查新版本
              - 帮助菜单 → 检查更新
              - 版本号比较，提示下载新版本
            [新增] 更新日志
              - 帮助菜单 → 更新日志
              - 内置完整版本历史
            [新增] 各模块列表底部添加按钮
              - 快速添加新条目，无需快捷键
            [新增] 记账模块（Ctrl+6）
              - 支持收入/支出记录，预设分类
              - Ctrl+Shift+S 语音播报当月收支统计
              - 详情区显示月度汇总
              - 加密存储，需解锁密码库
            [新增] 全量备份与恢复（Ctrl+Shift+A / Ctrl+Shift+R）
              - 一键导出所有数据为 .bnfull 文件
              - 支持从 .bnfull 文件恢复全部数据
            [优化] 状态栏显示所有模块快捷键提示

            v1.3 (2026-07-20)
            ─────────────────
            [新增] 记事本模块（Ctrl+2，原文案收藏）
              - 10,000 字内容限制
              - 复制内容到剪贴板
            [新增] 日记模块（Ctrl+4，原记事本）
              - 10,000 字内容限制
              - 天气和心情标签
              - 自动日期标题
              - 按月浏览（Ctrl+Shift+M）
              - 连续记录提醒
            [新增] 证件保存模块（Ctrl+5）
              - 加密存储电子证件
              - 支持证件图片
            [新增] 排序模式切换（Ctrl+Shift+O）
            [新增] 导出TXT（Ctrl+Shift+X）
            [新增] 模板插入（Ctrl+Shift+I）
            [新增] 收藏置顶（Ctrl+Shift+F）
            [新增] 批量删除和批量移动分类
            [新增] 自定义快捷键设置
            [新增] 重复检测
            [新增] 网址健康检查
            [新增] 审计日志
            [优化] 网址条目增加账号字段

            v1.2 (2026-06-15)
            ─────────────────
            [新增] 自动锁定（可配置超时时间）
            [新增] 防截屏保护
            [新增] 密码生成器（F5）
            [新增] TOTP 动态验证码（Ctrl+Shift+T）
            [新增] 字段选择器（Ctrl+Shift+V）
            [新增] 导入书签功能
            [新增] 加密备份与恢复
            [优化] 争渡读屏适配优化

            v1.1 (2026-05-10)
            ─────────────────
            [新增] 密码收藏模块（Ctrl+3）
              - AES-256-CBC 加密
              - PBKDF2-SHA256 密钥派生（600,000 次迭代）
              - 密码自动清除剪贴板
            [新增] 网址收藏模块（Ctrl+1）
              - Enter 打开网址
              - Ctrl+Enter 复制网址
            [新增] 分类树管理
            [新增] 全文搜索
            [新增] F6 焦点区域切换
            [优化] 纯键盘操作支持

            v1.0 (2026-04-01)
            ─────────────────
            [初始版本] 面向盲人用户的记事本与密码本
              - WPF + .NET 8 桌面应用
              - UI Automation 适配
              - 争渡读屏兼容
              - JSON 数据存储
            """;
    }

    /// <summary>获取指定版本的更新日志。找不到返回空字符串。</summary>
    public static string GetVersionChangelog(string version)
    {
        var full = GetFullChangelog();
        var marker = $"{version}";
        var idx = full.IndexOf(marker, StringComparison.OrdinalIgnoreCase);
        if (idx < 0) return "";

        // 找到下一个版本标记
        var nextIdx = full.IndexOf("\n\nv", idx + 1, StringComparison.OrdinalIgnoreCase);
        if (nextIdx < 0)
            return full[idx..].Trim();
        else
            return full[idx..nextIdx].Trim();
    }
}
